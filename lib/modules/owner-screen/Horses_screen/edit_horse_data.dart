import 'package:flutter/cupertino.dart';

class EditHorseDataScreen extends StatelessWidget {

  const EditHorseDataScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
